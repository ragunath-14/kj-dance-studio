# Production environment — baked in at Vite build time
# Frontend API & Socket.io connect to the custom domain
VITE_API_URL=/api

# In production, backend and frontend share the same origin — leave blank so socket uses window.location.origin
VITE_SOCKET_URL=
